# Docker compose: Flask+Redis и Prometheus+Grafana

## Содержимое
- `flask-redis/` – приложение со счётчиком посещений (Flask + Redis)
- `prometheus-grafana/` – стек мониторинга (Prometheus, Grafana, Blackbox)
